import {styled} from "@/styles";

export const HeaderContainer = styled('header', {
    position: 'relative',
    display: 'flex',
    flexDirection: 'row',
    paddingBottom: '2rem',
    paddingRight: 'calc(100vh - (1180px - 400px))',
    //padding: '2rem',
    //padding:
    width: '100%',
    maxWidth: 'calc(100vw - ((100vw - 1180px)/2))',
    marginLeft: 'auto',
    justifyContent: 'space-between',
    //overflow: 'hidden'
})
